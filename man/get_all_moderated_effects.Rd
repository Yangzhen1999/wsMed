% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Mo_get_all_moderated_effects.R
\name{get_all_moderated_effects}
\alias{get_all_moderated_effects}
\title{Compute Conditional Effects of Moderated Paths from Monte Carlo Results}
\usage{
get_all_moderated_effects(
  mc_result,
  W_method = c("discrete", "quantile"),
  W_values = c(-2, -1, 0, 1, 2),
  W_varname = "W1",
  data = NULL,
  ci_level = 0.95,
  digits = 3,
  JN = FALSE,
  W_range = NULL,
  resolution = 1000,
  alpha = 0.05,
  verbose = TRUE,
  return_empty_table = TRUE
)
}
\arguments{
\item{mc_result}{A \code{semmcci} object returned from \code{MC()} or \code{MCMI()}, containing Monte Carlo draws of model parameters.}

\item{W_method}{Character string. Method for specifying moderator values:
\code{"discrete"} (default) or \code{"quantile"}.}

\item{W_values}{Numeric vector. Moderator values at which to compute conditional effects.
Used only when \code{W_method = "discrete"}.}

\item{W_varname}{Character. Name of the moderator variable. Required for quantile-based extraction and JN plots.}

\item{data}{Optional data frame. Required if \code{W_method = "quantile"} or \code{JN = TRUE}.}

\item{ci_level}{Numeric. Confidence level for Monte Carlo confidence intervals. Default is 0.95.}

\item{digits}{Integer. Number of decimal places to round estimates. Default is 3.}

\item{JN}{Logical. Whether to compute Johnson-Neyman significance regions. Default is \code{FALSE}.}

\item{W_range}{Numeric vector of length 2. Range of moderator values to search for JN intervals. Default is \code{c(-3, 3)}.}

\item{resolution}{Integer. Number of equally spaced points within \code{W_range} to evaluate conditional effects for JN analysis. Default is 1000.}

\item{alpha}{Numeric. Significance level used in JN region computation. Default is 0.05.}

\item{verbose}{Logical. If \code{TRUE}, prints diagnostic messages. Default is \code{TRUE}.}
}
\value{
If \code{JN = FALSE}, returns a data frame with conditional estimates at each moderator level, with columns:
\itemize{
\item \code{Moderator}: Name of the moderator variable.
\item \code{Coefficient}: Name of the moderated parameter (e.g., "bw2_1").
\item \code{Path}: Base path name (e.g., "b2").
\item \code{Level}: Label for the moderator value (e.g., "-1", "50\%", etc.).
\item \code{W}: Numeric moderator value.
\item \code{Estimate}: Estimated conditional effect.
\item \code{SE}: Standard error of the conditional effect.
\item \code{CI.Lower}: Lower bound of the confidence interval.
\item \code{CI.Upper}: Upper bound of the confidence interval.
}

If \code{JN = TRUE}, returns a list with two elements:
\itemize{
\item \code{main}: The conditional effects table (same as above).
\item \code{JN}: A data frame summarizing the Johnson-Neyman significance region for each moderated parameter.
}
}
\description{
Extracts conditional effects for moderated path coefficients from a Monte Carlo simulation result
(typically produced by \code{semmcci::MC()} or \code{semmcci::MCMI()}). Supports both user-defined discrete values
and quantile-based moderator values. Optionally includes Johnson-Neyman intervals to identify the regions
where moderated effects are statistically significant.
}
\details{
This function identifies moderated parameters in a structural equation model (e.g., \code{"aw1_1"}, \code{"bw2_1"},
\code{"cpw1_1"}, \code{"dw_1_2_1"}) and evaluates the conditional effect of each path at different levels of the moderator variable.

Conditional effects are computed by combining the unmoderated (main) and moderated (interaction) coefficients via:
\deqn{ \theta(W) = \theta + \theta_{int} \cdot W }
where \eqn{\theta} is the main path coefficient, \eqn{\theta_{int}} is the interaction coefficient (e.g., \code{"bw2_1"}),
and \eqn{W} is the moderator value.

The function supports two approaches for choosing moderator values:
\itemize{
\item \code{"discrete"}: Use manually specified values (e.g., -2, -1, 0, 1, 2).
\item \code{"quantile"}: Automatically compute empirical quantiles (e.g., 5\%, 25\%, 50\%, 75\%, 95\%) of the moderator from data.
}

If \code{JN = TRUE}, the function also estimates Johnson-Neyman significance regions for each moderated path,
reporting the range of moderator values where the conditional effect is significantly different from zero.
}
\seealso{
\code{\link[=get_jn_results]{get_jn_results()}} for standalone JN region extraction.
}
