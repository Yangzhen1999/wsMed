# Create moderation output for wsMed

Create moderation output for wsMed

## Usage

``` r
.make_moderation(
  mc_res,
  data,
  W = NULL,
  MP = NULL,
  W_type = c("categorical", "continuous", "none"),
  alpha = 0.05,
  verbose = FALSE
)
```
