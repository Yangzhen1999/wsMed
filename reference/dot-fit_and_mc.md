# Fit SEM and run Monte-Carlo draws

Fit SEM and run Monte-Carlo draws

## Usage

``` r
.fit_and_mc(
  sem_model,
  data,
  Na = c("DE", "FIML"),
  R = 20000,
  alpha = 0.05,
  fixed.x = FALSE,
  verbose = TRUE,
  run_mc = TRUE
)
```
