# Assert a scalar (whole-number) integer with optional bounds

Assert a scalar (whole-number) integer with optional bounds

## Usage

``` r
assert_scalar_int(
  x,
  name = deparse(substitute(x)),
  lower = NULL,
  upper = NULL,
  allow_null = FALSE
)
```
